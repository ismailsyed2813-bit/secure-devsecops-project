apiVersion: v1
kind: Service

metadata:
  name: secure-node-service

spec:
  selector:
    app: secure-node-app

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30007

  type: NodePort